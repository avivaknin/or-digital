{
  "name": "digital-assistant",
  "version": "1.0.0",
  "description": "A simple and friendly guide for seniors to navigate computers and smartphones with ease.",
  "scripts": {
    "build": "esbuild index.tsx --bundle --splitting --format=esm --outdir=dist --platform=browser --define:process.env.NODE_ENV='\"production\"' && shx cp index.html dist/index.html && shx cp metadata.json dist/metadata.json"
  },
  "dependencies": {
    "@google/genai": "^1.14.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@netlify/functions": "^2.8.1",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.4",
    "@types/react": "^18.3.3",
    "shx": "^0.3.4"
  }
}